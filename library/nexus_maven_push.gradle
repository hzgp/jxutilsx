apply plugin: 'maven'

configurations {
  deployerJars
}

repositories {
  mavenCentral()
}

uploadArchives {
  repositories {
    mavenDeployer {
      repository(url: "http://192.168.1.18:8081/nexus/content/repositories/jxutils-x/") {
        authentication(userName: "admin", password: "admin123")
      }
      pom.project {
        version '1.1.3'
        artifactId 'jxutilsx'
        groupId 'com.jxkj.jxutilsx'
        packaging 'aar'
        description 'library for android'
      }
    }
  }
}